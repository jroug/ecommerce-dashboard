import { Plus,X } from "lucide-react";
import { useState } from "react";
export function CustomerTags({tags,onChange}:{tags:string[];onChange:(tags:string[])=>void}){const [value,setValue]=useState("");const add=()=>{const tag=value.trim();if(tag&&!tags.includes(tag))onChange([...tags,tag]);setValue("")};return <section className="admin-card"><header className="border-b px-4 py-3.5"><h2 className="text-[15px] font-semibold">Tags</h2></header><div className="p-4"><div className="flex gap-2"><input value={value} onChange={(event)=>setValue(event.target.value)} onKeyDown={(event)=>{if(event.key==="Enter"){event.preventDefault();add()}}} placeholder="Add a tag" className="admin-control h-8 min-w-0 flex-1 px-2 text-[12px] outline-none"/><button type="button" onClick={add} aria-label="Add tag" className="admin-control flex size-8 shrink-0 items-center justify-center"><Plus size={14}/></button></div><div className="mt-3 flex flex-wrap gap-1.5">{tags.length?tags.map((tag)=><span key={tag} className="inline-flex items-center gap-1 rounded-full bg-[var(--color-surface-hover)] px-2 py-1 text-[11px]">{tag}<button type="button" onClick={()=>onChange(tags.filter((item)=>item!==tag))} aria-label={`Remove ${tag}`}><X size={11}/></button></span>):<p className="text-[12px] text-[var(--color-text-muted)]">No tags</p>}</div></div></section>}
