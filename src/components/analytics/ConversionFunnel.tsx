import type { ConversionAnalytics } from "@/types/analytics";
export function ConversionFunnel({data}:{data:ConversionAnalytics}){const steps=[{label:"Sessions",value:data.sessions},{label:"Added to cart",value:data.addToCart},{label:"Reached checkout",value:data.checkout},{label:"Purchases",value:data.purchases}];return <section className="admin-card"><header className="border-b px-4 py-3.5"><h2 className="text-[15px] font-semibold">Conversion funnel</h2><p className="text-[11px] text-[var(--color-text-muted)]">Mock traffic analytics</p></header><div className="p-4"><div className="grid grid-cols-2 gap-3 sm:grid-cols-4">{steps.map((step,index)=><div key={step.label} className="rounded-[var(--radius-md)] bg-[var(--color-surface-subdued)] p-3"><p className="text-[11px] text-[var(--color-text-muted)]">{step.label}</p><p className="mt-1 text-[16px] font-semibold">{step.value.toLocaleString()}</p>{index>0&&<p className="text-[10px] text-[var(--color-text-muted)]">{((step.value/data.sessions)*100).toFixed(1)}% of sessions</p>}</div>)}</div><p className="mt-3 text-[12px]">Conversion rate <strong className="ml-1">{data.conversionRate.toFixed(2)}%</strong></p></div></section>}
