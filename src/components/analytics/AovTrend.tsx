"use client";
import { Line,LineChart,ResponsiveContainer,Tooltip,XAxis,YAxis } from "recharts";
import type { AnalyticsTimePoint } from "@/types/analytics";
const money=new Intl.NumberFormat("en-IE",{style:"currency",currency:"EUR"});
export function AovTrend({data,change}:{data:AnalyticsTimePoint[];change:number}){const current=data.length?data.reduce((sum,item)=>sum+item.aov,0)/data.length:0;return <section className="admin-card min-w-0"><header className="flex items-start justify-between border-b px-4 py-3.5"><div><h2 className="text-[15px] font-semibold">Average order value</h2><p className="mt-1 text-[18px] font-semibold">{money.format(current)}</p></div><span className="text-[11px] font-semibold text-[var(--color-success)]">+{change}%</span></header><div className="h-[150px] px-2 py-3"><ResponsiveContainer width="100%" height="100%"><LineChart data={data} margin={{left:-24,right:10}}><XAxis dataKey="label" tickLine={false} axisLine={false} tick={{fontSize:10,fill:"#8a8a8a"}}/><YAxis tickLine={false} axisLine={false} tick={{fontSize:10,fill:"#8a8a8a"}}/><Tooltip formatter={(value)=>[money.format(Number(value)),"AOV"]} contentStyle={{border:"1px solid #dedede",borderRadius:8,fontSize:12}}/><Line type="monotone" dataKey="aov" stroke="#5d7468" strokeWidth={2} dot={false}/></LineChart></ResponsiveContainer></div></section>}
